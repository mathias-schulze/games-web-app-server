management:
  endpoints:
    web:
      cors:
        allowed-origins: 
        allowed-methods: GET, POST, PATCH, DELETE

spring:
  messages:
    basename: messages,games.hero_realms.messages

firebase:
  firestore:
    database-url: https://msz-games.firebaseio.com
